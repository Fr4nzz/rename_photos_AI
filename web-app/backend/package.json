{
  "name": "ai-photo-processor-backend",
  "version": "1.0.0",
  "description": "Local backend for AI Photo Processor - RAW rotation & file rename",
  "main": "server.js",
  "bin": "server.js",
  "pkg": {
    "targets": ["node18-win-x64"],
    "assets": ["bin/**/*"],
    "outputPath": "dist"
  },
  "scripts": {
    "start": "node server.js",
    "build": "node build.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.0.0"
  }
}
